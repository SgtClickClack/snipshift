# Dependency directories
node_modules/
.npm/
.yarn/

# Build outputs (Massive generated files)
dist/
build/
.next/
out/

# Static Assets (AI doesn't need to read images)
public/images/
assets/
*.png
*.jpg
*.svg
*.ico

# Lock files (Huge and useless for logic checks)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Logs and local config
*.log
.env
.DS_Store

# Test artifacts and screenshots
tests/*.png
e2e-results/
playwright-report/

# Documentation tracking files (large markdown files)
# NOTE: We keep these *included* so we can append required dev log entries after each task.
# (They’re markdown, not generated artifacts.)
# DEVELOPMENT_TRACKING_PART_*.md

# Tree/structure dumps
tree_*.json
tree_*.txt

# Commit history dumps
commits*.json

# Old backup files
*_old.json

# API build artifacts
api/index.js
api/index.js.map
api/api/

# Drizzle migrations (SQL files are verbose)
api/drizzle/*.sql

# Test output files
*_output.txt
test_output.txt

# Recursive/Backup folders (CRITICAL - contains nested copies causing performance issues)
snipshift/

# Coverage reports
coverage/

# Test results
test-results/
playwright-report/