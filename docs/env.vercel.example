# Vercel Environment Variables (Example)
#
# NOTE:
# - Do NOT commit real secrets.
# - Values here are placeholders only.
#
# Frontend (Vite) variables must be prefixed with VITE_

# =========================
# Frontend (Vite)
# =========================
VITE_API_URL=https://your-domain.com/api

VITE_FIREBASE_API_KEY=your_firebase_api_key
# Auth domain for Google sign-in popup branding. 
# This determines what appears in the Google OAuth popup.
# Options:
#   - hospogo.com (default - branded custom domain)
#   - hospogo-75b04.firebaseapp.com (Firebase default - shows project name)
#   - hospogo-75b04.web.app (Firebase Hosting domain)
#   - www.hospogo.com (custom domain - requires Firebase custom domain setup)
VITE_FIREBASE_AUTH_DOMAIN=hospogo.com
VITE_FIREBASE_PROJECT_ID=hospogo-75b04
VITE_FIREBASE_STORAGE_BUCKET=hospogo-75b04.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id_optional

VITE_GOOGLE_MAPS_API_KEY=your_google_maps_key
# Google OAuth Client ID (from Google Cloud Console > APIs & Services > Credentials)
# This should match the Web Client ID configured in Firebase Authentication
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id.apps.googleusercontent.com
# Note: Firebase handles redirect URIs automatically via /__/auth/handler
# Do NOT set a custom redirect URI unless you have a specific use case

VITE_STRIPE_PUBLISHABLE_KEY=pk_live_or_pk_test_here

# Pusher Channels (Real-time messaging and events)
# Get these from https://dashboard.pusher.com/
VITE_PUSHER_APP_KEY=your_pusher_app_key
VITE_PUSHER_CLUSTER=us2

# =========================
# Backend (API)
# =========================
NODE_ENV=production
PORT=5000
FRONTEND_URL=https://your-domain.com

DATABASE_URL=postgresql://user:password@host:port/db?sslmode=require
POSTGRES_URL=postgresql://user:password@host:port/db?sslmode=require

STRIPE_SECRET_KEY=sk_live_or_sk_test_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Pusher Channels (Real-time messaging and events)
# Get these from https://dashboard.pusher.com/
PUSHER_APP_ID=your_pusher_app_id
PUSHER_KEY=your_pusher_key
PUSHER_SECRET=your_pusher_secret
PUSHER_CLUSTER=us2

RESEND_API_KEY=re_your_resend_key

# Firebase Admin (choose ONE approach)
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"...","client_email":"..."}
# OR split vars:
# FIREBASE_PROJECT_ID=your_project_id
# FIREBASE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
