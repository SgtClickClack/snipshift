/**
 * CalendarCell – shift block in the calendar grid
 *
 * Renders multiple avatars and a fraction (e.g. 2/5) based on `capacity` and
 * assigned staff. Re-uses ShiftBlock for all layout and styling.
 */

import { ShiftBlock, ShiftBlockProps } from './shift-block';

export type { ShiftBlockProps };
export { ShiftBlock as CalendarCell };
