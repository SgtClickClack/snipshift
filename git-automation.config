# Git Automation Configuration
# This file contains configuration for automated Git operations

# GitHub Configuration
GITHUB_USERNAME=RescueAdmin
GITHUB_EMAIL=your-email@example.com
GITHUB_REPOSITORY=SgtClickClack/snipshift

# Default Branch Configuration
DEFAULT_BRANCH=main
BACKUP_BRANCH=develop

# Automation Settings
AUTO_STASH=true
AUTO_COMMIT=false
CONFIRM_BEFORE_FORCE=true

# Logging Settings
LOG_LEVEL=INFO
LOG_FILE=git-automation.log

# Security Settings
CREDENTIAL_STORE=true
SSH_KEY_PATH=~/.ssh/id_ed25519

# Instructions for setup:
# 1. Replace 'your-email@example.com' with your actual GitHub email
# 2. If using Personal Access Token, configure it with:
#    git config credential.helper store
# 3. If using SSH, ensure your SSH key is added to GitHub
# 4. Test the setup with: .\git-automation.ps1 status
