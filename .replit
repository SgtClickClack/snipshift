# This command will be executed by the main "Run" button.
# It starts the entire new SnipShift platform using Docker.
run = "cd snipshift-next && docker-compose up --build"
# Language configuration
language = "bash"
entrypoint = "main.sh"
